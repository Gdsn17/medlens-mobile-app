import React from 'react';

// COMPLETE FONT LOADING ELIMINATION
// No font loading needed - using only emoji/text icons
const FontLoader = ({ children }) => {
  return children;
};

export default FontLoader;